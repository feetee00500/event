DATABASE_URL="postgresql://postgres:postgres@localhost:5432/event_tird?schema=public"
AUTH_SECRET="replace-with-a-long-random-secret"
NEXTAUTH_URL="http://localhost:3000"
NEXT_PUBLIC_APP_URL="http://localhost:3000"

# Local development only. Set to false to test the real login flow.
DEV_AUTH_BYPASS="true"

# Optional: keep uploads in an object storage provider in production.
NEXT_PUBLIC_DEFAULT_EVENT_IMAGE=""
