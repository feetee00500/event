"use client";

import { useEffect, useRef, useState } from "react";
import type { FormEvent } from "react";
import { Ban, Camera, CameraOff, CheckCircle2, ChevronDown, CircleAlert, Clock3, Flashlight, Keyboard, Loader2, Maximize2, Minimize2, RefreshCw, ShieldAlert, Wifi, WifiOff, XCircle } from "lucide-react";
import { Button } from "@/components/ui/button";
import { Card, CardContent, CardHeader } from "@/components/ui/card";
import { Field, Input, Select } from "@/components/ui/field";
import { InlineNotice } from "@/components/ui/feedback";

type Gate = { id: string; name: string };
type Result = { success: boolean; status: string; message: string; attendee?: { name: string; ticketType: string; ticketNumber: string }; checkedInAt?: string; gate?: { name: string }; firstCheckedInAt?: string; gateName?: string; checkinOpenAt?: string; checkinCloseAt?: string };
type CameraDevice = { id: string; label: string };
type ScannerControls = { stop: () => void };

const resultLabels: Record<string, { title: string; detail: string; tone: "success" | "warning" | "error"; icon: typeof CheckCircle2 }> = {
  CHECKED_IN: { title: "Check-in สำเร็จ", detail: "บันทึกการเข้างานแล้ว", tone: "success", icon: CheckCircle2 },
  ALREADY_CHECKED_IN: { title: "QR นี้ถูกใช้งานแล้ว", detail: "ระบบไม่บันทึกการเข้างานซ้ำ", tone: "warning", icon: ShieldAlert },
  INVALID_TOKEN: { title: "QR ไม่ถูกต้อง", detail: "ไม่พบ Ticket ที่ตรงกับ QR นี้", tone: "error", icon: XCircle },
  CANCELLED: { title: "บัตรถูกยกเลิก", detail: "ไม่สามารถใช้บัตรนี้เข้างานได้", tone: "error", icon: Ban },
  EXPIRED: { title: "บัตรหมดอายุ", detail: "Ticket นี้ไม่อยู่ในช่วงใช้งาน", tone: "error", icon: Clock3 },
  TOO_EARLY: { title: "ยังไม่ถึงเวลา Check-in", detail: "ระบบจะเปิดรับตามเวลาของกิจกรรม", tone: "warning", icon: Clock3 },
  TOO_LATE: { title: "ปิด Check-in แล้ว", detail: "ช่วงเวลารับเข้างานสิ้นสุดแล้ว", tone: "error", icon: Clock3 },
  EVENT_MISMATCH: { title: "QR เป็นของคนละกิจกรรม", detail: "เลือก Event ให้ตรงกับบัตรก่อนสแกน", tone: "error", icon: ShieldAlert },
};

export function ScannerClient({ eventId, eventName, gates }: { eventId: string; eventName: string; gates: Gate[] }) {
  const videoRef = useRef<HTMLVideoElement>(null);
  const frameRef = useRef<HTMLDivElement>(null);
  const controlsRef = useRef<ScannerControls | null>(null);
  const lastScanRef = useRef({ token: "", at: 0 });
  const audioContextRef = useRef<AudioContext | null>(null);
  const deviceIdRef = useRef("browser");
  const busyRef = useRef(false);
  const scannerStartingRef = useRef(false);
  const mountedRef = useRef(false);
  const scannerSessionRef = useRef(0);
  const [gateId, setGateId] = useState(gates[0]?.id ?? "");
  const [running, setRunning] = useState(false);
  const [manualOpen, setManualOpen] = useState(false);
  const [manual, setManual] = useState("");
  const [result, setResult] = useState<Result | null>(null);
  const [busy, setBusy] = useState(false);
  const [cameraError, setCameraError] = useState("");
  const [cameraNotice, setCameraNotice] = useState("");
  const [online, setOnline] = useState(true);
  const [torch, setTorch] = useState(false);
  const [cameraDevices, setCameraDevices] = useState<CameraDevice[]>([]);
  const [cameraId, setCameraId] = useState("");
  const [fullscreen, setFullscreen] = useState(false);
  const [sessionCount, setSessionCount] = useState(0);
  const [lastScan, setLastScan] = useState<{ name: string; at: string } | null>(null);

  useEffect(() => { mountedRef.current = true; try { const storedDeviceId = window.localStorage.getItem("event-tird-device:v1"); const generatedDeviceId = window.crypto.randomUUID?.() ?? `browser-${Date.now()}`; deviceIdRef.current = storedDeviceId ?? generatedDeviceId; if (!storedDeviceId) window.localStorage.setItem("event-tird-device:v1", deviceIdRef.current); } catch { deviceIdRef.current = `browser-${Date.now()}`; } setOnline(navigator.onLine); const on = () => setOnline(true); const off = () => setOnline(false); window.addEventListener("online", on); window.addEventListener("offline", off); return () => { mountedRef.current = false; scannerSessionRef.current += 1; window.removeEventListener("online", on); window.removeEventListener("offline", off); controlsRef.current?.stop(); controlsRef.current = null; void audioContextRef.current?.close(); audioContextRef.current = null; }; }, []);

  useEffect(() => { const onFullscreen = () => setFullscreen(Boolean(document.fullscreenElement)); document.addEventListener("fullscreenchange", onFullscreen); return () => document.removeEventListener("fullscreenchange", onFullscreen); }, []);

  async function submitToken(token: string) {
    if (!gateId || busyRef.current) return;
    if (!online) { setResult({ success: false, status: "OFFLINE", message: "อุปกรณ์ออฟไลน์ ไม่สามารถบันทึก Check-in ได้" }); return; }
    const now = Date.now(); if (lastScanRef.current.token === token && now - lastScanRef.current.at < 2500) return; lastScanRef.current = { token, at: now }; busyRef.current = true; setBusy(true); controlsRef.current?.stop(); controlsRef.current = null; setRunning(false); setCameraNotice("");
    try { const response = await fetch("/api/checkin", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ token, eventId, gateId, deviceId: deviceIdRef.current }) }); const body = await response.json().catch(() => ({})); const nextResult: Result = response.ok || body.status ? body as Result : { success: false, status: "ERROR", message: body.error ?? "ไม่สามารถตรวจสอบ QR ได้" }; setResult(nextResult); if (nextResult.success) { setSessionCount((count) => count + 1); setLastScan({ name: nextResult.attendee?.name ?? "ผู้เข้าร่วม", at: new Date().toISOString() }); } beep(Boolean(nextResult.success)); } catch { setResult({ success: false, status: "NETWORK_ERROR", message: "เชื่อมต่อระบบไม่ได้ กรุณาตรวจสอบเครือข่ายแล้วลองใหม่" }); } finally { busyRef.current = false; setBusy(false); }
  }

  async function refreshCameras() { if (!navigator.mediaDevices?.enumerateDevices) return; const devices = await navigator.mediaDevices.enumerateDevices(); const next = devices.filter((device) => device.kind === "videoinput").map((device, index) => ({ id: device.deviceId, label: device.label || `กล้อง ${index + 1}` })); setCameraDevices(next); if (!cameraId && next[0]) setCameraId(next[0].id); }
  async function startScanner(selectedCameraId = cameraId) { if (scannerStartingRef.current || controlsRef.current) return; setCameraError(""); setCameraNotice(""); setResult(null); if (!videoRef.current) return; if (!gateId) { setCameraError("กรุณาเลือก Gate ก่อนเปิดกล้อง"); return; } scannerStartingRef.current = true; const session = ++scannerSessionRef.current; try { const { BrowserQRCodeReader } = await import("@zxing/browser"); const reader = new BrowserQRCodeReader(undefined, { delayBetweenScanAttempts: 400, delayBetweenScanSuccess: 750 }); const controls = await reader.decodeFromVideoDevice(selectedCameraId || undefined, videoRef.current, (scan) => { if (scan) void submitToken(scan.getText()); }); if (!mountedRef.current || session !== scannerSessionRef.current) { controls.stop(); return; } controlsRef.current = controls; setRunning(true); await refreshCameras(); } catch { if (mountedRef.current && session === scannerSessionRef.current) { setCameraError("ไม่สามารถเปิดกล้องได้ กรุณาอนุญาตการเข้าถึงกล้อง และใช้ localhost หรือ HTTPS"); setRunning(false); } } finally { scannerStartingRef.current = false; } }
  function stopScanner() { scannerSessionRef.current += 1; controlsRef.current?.stop(); controlsRef.current = null; setTorch(false); setRunning(false); }
  async function switchCamera() { if (cameraDevices.length < 2) return; const currentIndex = cameraDevices.findIndex((camera) => camera.id === cameraId); const next = cameraDevices[(currentIndex + 1) % cameraDevices.length]; if (!next) return; setCameraId(next.id); stopScanner(); await startScanner(next.id); }
  async function toggleTorch() { const track = (videoRef.current?.srcObject as MediaStream | null)?.getVideoTracks()[0]; const capabilities = track?.getCapabilities() as (MediaTrackCapabilities & { torch?: boolean }) | undefined; if (!track || !capabilities?.torch) { setCameraNotice("กล้องนี้ไม่รองรับไฟแฟลช หรือเบราว์เซอร์ไม่อนุญาตให้ควบคุมไฟ"); return; } const next = !torch; try { await track.applyConstraints({ advanced: [{ torch: next }] } as unknown as MediaTrackConstraints); setTorch(next); setCameraNotice(""); } catch { setCameraNotice("ไม่สามารถเปลี่ยนไฟแฟลชของกล้องนี้ได้"); } }
  async function toggleFullscreen() { if (!frameRef.current) return; if (document.fullscreenElement) await document.exitFullscreen(); else await frameRef.current.requestFullscreen(); }
  function beep(success: boolean) { try { const AudioContextClass = window.AudioContext || (window as unknown as { webkitAudioContext: typeof AudioContext }).webkitAudioContext; const context = audioContextRef.current ?? new AudioContextClass(); audioContextRef.current = context; const oscillator = context.createOscillator(); const gain = context.createGain(); oscillator.frequency.value = success ? 760 : 180; gain.gain.value = 0.05; oscillator.connect(gain); gain.connect(context.destination); oscillator.start(); oscillator.stop(context.currentTime + 0.12); } catch { /* Audio is optional and may be blocked by browser policy. */ } }
  async function submitManual(event: FormEvent<HTMLFormElement>) { event.preventDefault(); if (manual.trim()) await submitManualToken(manual.trim()); }
  async function submitManualToken(ticketNumber: string) { if (!gateId || busyRef.current) return; if (!online) { setResult({ success: false, status: "OFFLINE", message: "อุปกรณ์ออฟไลน์ ไม่สามารถบันทึก Check-in ได้" }); return; } busyRef.current = true; setBusy(true); stopScanner(); try { const response = await fetch("/api/checkin/manual", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ ticketNumber, eventId, gateId, deviceId: deviceIdRef.current }) }); const body = await response.json().catch(() => ({})); const nextResult: Result = response.ok || body.status ? body as Result : { success: false, status: "ERROR", message: body.error ?? "ไม่สามารถตรวจสอบ Ticket ได้" }; setResult(nextResult); if (nextResult.success) { setSessionCount((count) => count + 1); setLastScan({ name: nextResult.attendee?.name ?? ticketNumber, at: new Date().toISOString() }); } beep(Boolean(nextResult.success)); } catch { setResult({ success: false, status: "NETWORK_ERROR", message: "เชื่อมต่อระบบไม่ได้ กรุณาตรวจสอบเครือข่ายแล้วลองใหม่" }); } finally { busyRef.current = false; setManual(""); setManualOpen(false); setBusy(false); } }
  const gateName = gates.find((gate) => gate.id === gateId)?.name ?? "ยังไม่ได้เลือก Gate";
  const cameraLabel = cameraDevices.find((camera) => camera.id === cameraId)?.label ?? "กล้องหลัก";

  return <div className="space-y-5"><Card><CardHeader><div className="flex flex-col gap-4 sm:flex-row sm:items-center sm:justify-between"><div><p className="eyebrow">Scanner mode</p><h1 className="mt-1 text-xl font-bold">{eventName}</h1><p className="mt-1 text-sm text-muted">ตรวจที่ <span className="font-semibold text-ink">{gateName}</span></p></div><div className="flex flex-wrap items-center gap-3 text-xs font-semibold"><span className={`flex items-center gap-1.5 ${online ? "text-success" : "text-danger"}`}><span className={`h-2 w-2 rounded-full ${online ? "bg-success" : "bg-danger"}`} />{online ? <><Wifi size={15} />Online</> : <><WifiOff size={15} />Offline</>}</span><span className="rounded-full bg-[#eef6ff] px-2.5 py-1 text-primary">รอบนี้ {sessionCount} คน</span></div></div></CardHeader><CardContent><div className="grid gap-4 sm:grid-cols-2"><Field label="Gate ที่ตรวจ"><Select value={gateId} onChange={(e) => setGateId(e.target.value)} disabled={running}>{gates.length ? gates.map((gate) => <option key={gate.id} value={gate.id}>{gate.name}</option>) : <option value="">ยังไม่มี Gate</option>}</Select></Field>{cameraDevices.length > 1 ? <Field label="กล้อง"><Select value={cameraId} onChange={(e) => setCameraId(e.target.value)} disabled={running}>{cameraDevices.map((camera) => <option key={camera.id} value={camera.id}>{camera.label}</option>)}</Select></Field> : null}</div>{!gates.length ? <div className="mt-4"><InlineNotice tone="info">กิจกรรมนี้ยังไม่มี Gate ที่เปิดใช้งาน กรุณาสร้าง Gate ก่อนเริ่มสแกน</InlineNotice></div> : null}{cameraError ? <div className="mt-4"><InlineNotice tone="error">{cameraError}</InlineNotice></div> : null}{cameraNotice ? <div className="mt-4"><InlineNotice tone="info">{cameraNotice}</InlineNotice></div> : null}<div ref={frameRef} className="relative mt-5 overflow-hidden rounded-md bg-ink"><video ref={videoRef} className="aspect-[4/3] w-full object-cover" muted playsInline aria-label={`ภาพจากกล้องสำหรับสแกน QR ${cameraLabel}`} />{!running ? <div className="absolute inset-0 flex flex-col items-center justify-center bg-ink px-6 text-center text-white"><Camera size={38} className="text-[#22C7AE]" /><p className="mt-4 font-semibold">พร้อมสแกน QR Code</p><p className="mt-1 text-sm text-[#A9BFD3]">จัด QR ให้อยู่ในกรอบกล้อง</p></div> : <div className="pointer-events-none absolute inset-0 flex items-center justify-center"><div className="h-52 w-52 rounded-md border-2 border-[#22C7AE] shadow-[0_0_0_999px_rgba(36,36,36,0.38)]" /></div>}<div className="absolute bottom-4 left-1/2 flex -translate-x-1/2 flex-wrap justify-center gap-2">{running ? <Button variant="secondary" onClick={stopScanner}><CameraOff size={17} />หยุดกล้อง</Button> : <Button variant="secondary" className="bg-white text-ink hover:bg-paper" onClick={() => void startScanner()} disabled={!gateId || !gates.length || busy}><Camera size={17} />เปิดกล้อง</Button>}{running && cameraDevices.length > 1 ? <Button variant="secondary" onClick={() => void switchCamera()}><RefreshCw size={17} />สลับกล้อง</Button> : null}{running ? <Button variant="secondary" aria-label="สลับไฟแฟลช" onClick={() => void toggleTorch()}><Flashlight size={17} />{torch ? "ปิดไฟ" : "ไฟ"}</Button> : null}<Button variant="secondary" aria-label={fullscreen ? "ออกจากเต็มจอ" : "เปิดเต็มจอ"} onClick={() => void toggleFullscreen}>{fullscreen ? <Minimize2 size={17} /> : <Maximize2 size={17} />}{fullscreen ? "ย่อ" : "เต็มจอ"}</Button></div></div><div className="mt-4 flex flex-col gap-2 sm:flex-row"><Button variant="secondary" className="flex-1" onClick={() => setManualOpen((value) => !value)}><Keyboard size={17} />กรอก Ticket Number เอง<ChevronDown size={15} /></Button><Button variant="ghost" onClick={() => { setResult(null); setCameraError(""); }}><RefreshCw size={16} />ล้างผลลัพธ์</Button></div>{manualOpen ? <form className="mt-4 flex flex-col gap-2 sm:flex-row" onSubmit={(event) => void submitManual(event)}><Input value={manual} onChange={(e) => setManual(e.target.value)} placeholder="เช่น EVT-2026-AB12CD34" autoFocus /><Button type="submit" disabled={!manual.trim() || busy}>{busy ? <Loader2 size={16} className="animate-spin" /> : null}ตรวจสอบ</Button></form> : null}</CardContent></Card>{lastScan ? <div className="flex flex-col gap-1 rounded-lg border border-line bg-white px-4 py-3 text-sm sm:flex-row sm:items-center sm:justify-between"><span className="text-muted">สแกนสำเร็จล่าสุด: <strong className="text-ink">{lastScan.name}</strong></span><span className="mono text-xs text-muted">{new Intl.DateTimeFormat("th-TH", { dateStyle: "short", timeStyle: "short", timeZone: "Asia/Bangkok" }).format(new Date(lastScan.at))}</span></div> : null}{result ? <ScanResult result={result} onNext={() => { setResult(null); void startScanner(); }} /> : <div className="rounded-sm border border-dashed border-line px-5 py-4 text-center text-sm text-muted" aria-live="polite">ผลการตรวจสอบจะแสดงที่นี่ทันทีหลังสแกน</div>}</div>;
}

function ScanResult({ result, onNext }: { result: Result; onNext: () => void }) { const config = resultLabels[result.status] ?? { title: result.success ? "Check-in สำเร็จ" : "ตรวจสอบไม่สำเร็จ", detail: result.message, tone: result.success ? "success" : "error", icon: result.success ? CheckCircle2 : CircleAlert }; const Icon = config.icon; const toneClass = config.tone === "success" ? { card: "border-[#b8d8ff]", icon: "bg-[#e4faf3] text-[#0761d1]", title: "text-[#0761d1]" } : config.tone === "warning" ? { card: "border-[#f1df9f]", icon: "bg-[#ffefcf] text-[#8a6500]", title: "text-[#8a4b09]" } : { card: "border-[#f4b7bb]", icon: "bg-[#fff4f4] text-danger", title: "text-danger" }; return <Card className={toneClass.card}><CardContent><div className="flex items-start gap-3"><span className={`flex h-10 w-10 shrink-0 items-center justify-center rounded-full ${toneClass.icon}`}><Icon size={22} /></span><div><p className={`font-bold ${toneClass.title}`}>{config.title}</p><p className="mt-1 text-sm text-muted">{config.detail}</p>{result.success && result.attendee ? <div className="mt-3 grid gap-2 text-sm sm:grid-cols-2"><p><span className="text-muted">ผู้เข้าร่วม: </span><strong>{result.attendee.name}</strong></p><p><span className="text-muted">ประเภท: </span><strong>{result.attendee.ticketType}</strong></p><p><span className="text-muted">เวลา: </span><strong>{result.checkedInAt ? new Intl.DateTimeFormat("th-TH", { timeStyle: "short", dateStyle: "medium", timeZone: "Asia/Bangkok" }).format(new Date(result.checkedInAt)) : "—"}</strong></p><p><span className="text-muted">Gate: </span><strong>{result.gate?.name ?? "—"}</strong></p></div> : null}{!result.success && result.status === "ALREADY_CHECKED_IN" && result.firstCheckedInAt ? <p className="mt-3 text-sm text-muted">Check-in ครั้งแรก {new Intl.DateTimeFormat("th-TH", { dateStyle: "medium", timeStyle: "short", timeZone: "Asia/Bangkok" }).format(new Date(result.firstCheckedInAt))} · {result.gateName ?? "ไม่ทราบ Gate"}</p> : null}{result.status === "TOO_EARLY" && result.checkinOpenAt ? <p className="mt-3 text-sm text-muted">เปิด Check-in เวลา {new Intl.DateTimeFormat("th-TH", { dateStyle: "medium", timeStyle: "short", timeZone: "Asia/Bangkok" }).format(new Date(result.checkinOpenAt))}</p> : null}</div></div><div className="mt-5 flex flex-col gap-2 sm:flex-row"><Button className="flex-1" onClick={onNext}><Camera size={17} />สแกนถัดไป</Button><Button variant="secondary" onClick={() => window.setTimeout(() => window.location.reload(), 0)}><RefreshCw size={16} />รีเฟรชหน้า</Button></div></CardContent></Card>; }